---
driver:
  name: exec

transport:
  name: exec

provisioner:
  name: chef_infra
  chef_omnibus_install: false
  chef_license: accept-no-persist
  deprecations_as_errors: true

verifier:
  name: inspec

platforms:
  - name: windows-latest
